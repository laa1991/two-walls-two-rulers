# 发布红线（DATA-POLICY）

> 这一页**先于**任何产物写定。它的每一条都是**可检查**的（下附检查命令）；产物离开本机之前，逐条过一遍。

## 一、绝不发布（硬红线）

1. **会话原文**：`session.jsonl.zstd` / 压缩摘要原文（`compaction/summary` 的 `data.summary[].text`）/ 逐字件（`checkpoint-*.md`）—— 一律不发布、不进报告引文。
   - 理由：摘要里含**私人生活、岗位、公司名、家庭与健康信息**；它不是我们有权公开的东西。
2. **可定位到人的字段**：会话 id、标题、工作区路径、`~/.dsh` 下的任何路径、微信/邮件/凭据字样。
3. **未公开的第三方材料**：别人的论文 PDF / 抽取件（可引用公开的 arXiv 链接与页码，不重发全文）。

## 二、可以发布（照实做）

1. **代码**：四个装置 + 判据（阈值写在头注释里）。
2. **聚合读数**：每会话**一行**——代数、失败分类与次数、遮蔽 token 区间、逐字/语义存活率、**脱敏后的会话代号**（`S01..S48`，映射表留在本机）。
3. **我们自己写的结论与图表**：可以由聚合数直接画出。
4. **引文**：只引**别人公开发表的**（arXiv 链接 + 位置），以及**我们自己的判断**。

## 三、数字口径（防「把两把尺当一把」）

- 报「留存率」时**必须**同时写口径与分母：`逐字（5-gram ≥ 0.8，归一化后）` 或 `语义（零历史窗口三态：原样 / 改措辞但还在 / 没了）`；缺一项 = 不合格。
- 报失败数时写**去重口径**（同段对话占多个目录的情况）。
- 报「谁说的」时，若该结论来自子会话回报，标注**核过几处**（默认「未逐字复核」）。

## 四、自检（离开本机前跑一遍）

```powershell
# 1) 红线：一条命令 + 一份白名单（pattern 与豁免规则都固化在装置里，别在别处另写一份）
node tools/check-redline.mjs          # 任一**未豁免**命中 ⇒ exit 3；报告里贴它的输出即可
# 2) 交叉引用与家目录路径：文中点到的路径必须真实存在
node tools/check-tree-refs.mjs
# 3) 行数自检：报告里带数字的行数 == data/ 里的行数（缺一行只许写「未回」）
```

**豁免怎么声明**（这样外部跑出不同数字时，差在哪一眼可见）：
- **行级**：那一行行尾写 `redline-ok: <理由>`，**理由非空**才算豁免（空理由 = 命中）；
- **文件级**：只有两个 —— `tools/check-redline.mjs` 自己（pattern 拼着写在里面）与 `DATA-POLICY.md`（规则正文）。
- **默认目录那条例外要收紧**（2026-09-20，外部复核点出）：只有**非个人**的工具默认目录才算（形如 `~/.dsh/<tool>`，如 `~/.dsh/survival`）；**任何含个人名的路径都不豁免**（写成 `<人设目录>/…` 的那种），得改成「由调用方传目录 / 读环境变量」。
- 产出的**扫描面**（文件数/行数）与命中数一起打印 —— 「扫得少」和「扫得干净」是两件事，别混读。

⚠️ 上面每条都要打印出**成功那一句**才算过；只看退出码会把「模块没找到」读成「判据通过」。
